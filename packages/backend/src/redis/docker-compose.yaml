services:
  redis:
    image: valkey/valkey:9.0-alpine3.24
    container_name: glugg-valkey
    ports:
      - '6379:6379'
    volumes:
      - valkey_data:/data
    command: valkey-server --requirepass secret --appendonly yes
    restart: always

volumes:
  valkey_data:
